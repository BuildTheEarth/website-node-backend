{
  "openapi": "3.0.2",
  "info": {
    "title": "BuildTheEarth.net API",
    "version": "1.0"
  },
  "security": [
    {"name": []}
  ],
  "components": {
    "securitySchemes": {
      "jwt": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "servers": [
    {
      "url": "https://api.buildthe.earth/v1",
      "description": "Development instance"
    },
    {
      "url": "https://api.buildtheearth.net/v1",
        "description": "Production instance"
    }
  ],
  "tags": [{
    "name": "Buildteams",
    "description": "Anything regarding the buildteams"
  }],
  "paths": {
    "/buildteams": {
      "get": {
        "description": "Get a list of all build teams",
		  "parameters": [
            {
                "description": "The page of results to return, **starting at 0**",
                "in": "query",
                "name": "page",
                "required": false,
                "schema": {
                  "type": "integer"
                }
            }
          ],
		  "responses": {
          "200": {
            "description": "OK"
          }
        },
        "tags": ["Buildteams"]
      }
    }

  }
}
